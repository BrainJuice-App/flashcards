// import { useContext, useEffect, useState } from 'react';

// import { ProfileContext } from '../context/ProfileContext';

// import { getProfile } from '../services/profile';
// // useProfile will contain dispatch functions for our profilecontext, and I think we're gonna put the useEffect for like profile here
// export function useProfile() {
//   const { dispatch } = useContext(ProfileContext);
//   const { user } = useUser();

//   const currentProfile = async (user.id) => {
//       // fetchprofilebyid, make that the payload for our dispatch function that will set the state aka profile as the payload.first name
//   }
// }
